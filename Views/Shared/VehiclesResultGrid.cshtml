@model Domain.Car.PaginatedList<Domain.Car.VehicleByMakeDto>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Vehicle Type Name</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Items.Any())
        {
            foreach (var vehicle in Model.Items)
            {
                <tr>
                    <td>@vehicle.VehicleTypeName</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="1" class="text-center">No vehicles found.</td>
            </tr>
        }
    </tbody>
</table>

<!-- Pagination Controls -->
<div class="mt-3">
    <nav>
        <ul class="pagination justify-content-center">
            @for (int i = 1; i <= Model?.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                    <a class="page-link" href="#" onclick="loadVehicles(@i);">@i</a>
                </li>
            }
        </ul>
    </nav>
</div>
